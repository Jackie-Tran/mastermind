import React, { Component } from 'react'

export default class BigCircle extends Component {
    render() {
        return (
            <button style={style}/>
        )
    }
}

const style = {
}
